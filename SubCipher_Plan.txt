substitution_encrypt:
	if ciph_alph_checker(cipher_alph)
		open files
	else
		terminate
		
	base_list = regular_alphabet
	ciph_alph_list = ciph_alph_to_lst(key)
	cipher_list = []
	
	for each letter in ciph_alph:
		append ciph_alph to cipher_list
		
	for element in plaintext_file:
		grab base_list index of element
		(use index to grab element from ciph_alph)
		append new element to ciph_list
	
	concatenate ciph_list into string
	
	write string to file
	
		
	
		
	
key_checker:
	if no duplicates:
		return TRUE
	else:
		return FALSE
		
key_to_list:
	key_lst = []
	for elmt in key:
		append key to key
	return key_lst
	
